#!/bin/bash

timestamp="$(date --rfc-3339=seconds | sed 's/[^0-9]//g')"
backup_path="{{ partner_codes_backups_dir }}/dehydrated-${timestamp}.tar.bz2"

tar cjf ${backup_path} {{ partner_codes_tls_path }}
journalctl --vacuum-size 1
